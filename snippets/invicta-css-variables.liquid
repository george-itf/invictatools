{% comment %}
  ============================================================
  INVICTA DESIGN TOKENS v2.0 (CONSOLIDATED)
  ============================================================
  Single source of truth for all Invicta brand tokens.
  Include in theme.liquid BEFORE base.css:
  {% render 'invicta-css-variables' %}

  CHANGELOG:
  v2.0 - Consolidated from base.css and invicta-css-variables.liquid
       - Standardized naming convention (--inv-*)
       - Added duration, easing, z-index, and spacing tokens
       - Resolved color conflicts (#e11d26 is the canonical brand red)
  v2.1 - Moved all static tokens to assets/invicta-css-variables.css
       - All values are hardcoded (no Liquid dependencies), so the
         entire file is externalized for browser caching.
  ============================================================
{% endcomment %}

{{ 'invicta-css-variables.css' | asset_url | stylesheet_tag }}
